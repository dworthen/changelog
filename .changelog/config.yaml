currentVersion: 0.4.1
changelogFile: CHANGELOG.md
git:
  mainBranch: main
versionFiles:
  - path: package.json
    pattern: :\s*"(\d+\.\d+\.\d+)"
  - path: pyproject.toml
    pattern: version\s*=\s*"(\d+\.\d+\.\d+)"
apply:
  postCommands:
    - bun install
